1. Create a Version
git init
git status
git add .
git commit - "Message"

2. View Previous Versions
git log 
git log --all
git log --all --graph
git checkout <commit_hash>
git checkout <branch_name> (master or head branch)

3. Restore to Previous Version
git checkout <hash|branch> <file|folder> (ex. config.js)
git commit -m "Message";

SOME SHORTCUT

git config --global alias.s "status" - make s as the "status" shortcut